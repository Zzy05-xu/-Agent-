# ═══════════════════════════════════════════════════════════════
# 实习求职智能助手 Agent - 项目依赖
# Python 3.10+
# 安装方式: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# LangChain 核心（严格锁定版本保证兼容性）
langchain==0.2.11
langchain-community==0.2.10
langchain-openai==0.1.19

# 向量数据库（轻量级，无需服务部署）
faiss-cpu==1.8.0.post1

# Web UI
streamlit==1.36.0

# PDF 解析
pypdf==4.3.1

# 数据处理
pandas==2.2.2

# 环境变量管理
python-dotenv==1.0.1

# ═══════════════════════════════════════════════════════════════
# 以下为可选依赖（按需安装一个即可）
# ═══════════════════════════════════════════════════════════════

# 方案A（推荐）: 本地 Embedding 模型，零费用
# pip install sentence-transformers

# 方案B: 使用在线 Embedding API，在 .env 中配置 EMBEDDING_API_KEY
# 无需额外安装依赖
